# Lab — TODO для аудита репозитория (отдельная сессия, после 2Checkout)

## ⚠️ Временно убрано из публикации (2026-06-24) — ВЕРНУТЬ при аудите
Причина: деплой GitHub Pages падал по таймауту — репозиторий Lab разросся (206 МБ),
Pages не успевал залить весь объём за лимит ~10 мин. Чтобы быстро разблокировать публикацию
Calm Bed, самую тяжёлую старую папку убрали из git, **но оставили локально на диске**.

- **`visual-lab/`** (106 МБ — половина репо; старые S3-эксперименты генерации фото)
  - Сделано: `git rm -r --cached visual-lab/` + добавлено в `.gitignore`.
  - **Файлы НЕ удалены — лежат локально** в `movaro-website-lab/visual-lab/`.
  - Из git-истории тоже восстановимо: `git checkout <commit-before> -- visual-lab/`.
  - Ссылки-история на visual-lab на главной Lab временно ведут в 404 (ок, старое).

### Как вернуть (если решим, что нужно)
1. Убрать строку `visual-lab/` из `.gitignore`.
2. `git add visual-lab/ && git commit && git push`.
   (или перенести visual-lab в ОТДЕЛЬНЫЙ репозиторий — идея Marina, решим при аудите.)

## План аудита (Marina, договорённость 2026-06-24)
После того как сайт причешем и отправим на 2Checkout — **отдельной сессией**:
1. Аудит папки департамента + репозитория: навести порядок, выстроить структуру под масштабирование
   (много продуктов впереди).
2. Решить судьбу тяжёлых старых папок: `visual-lab` (106М), `stage-3.0` (22М), `stage-3.1.1` (29М) и пр.
   — что оставляем, что в отдельный репо/архив, что чистим.
3. Возможно: вынести тяжёлые фото-артефакты в отдельный репозиторий, чтобы основной Lab был лёгкий
   и деплой всегда быстрый.
